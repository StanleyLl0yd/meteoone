# MeteoOne 0.1.0-alpha.1

Status: canonical unified APK+AAB release bundle produced and verified; manual device acceptance **PASS**; pending manual RuStore closed-alpha setup/upload/moderation.

This is the first installable MeteoOne product slice. It intentionally stops before M3 product-UI expansion and polish.

## Included

- multi-source fused hourly forecast through the canonical 72-hour horizon;
- direct official NOAA/NCEP GFS, ECMWF IFS Open Data and DWD ICON source paths plus Open-Meteo forecast paths established by M1;
- Room-backed offline forecast cache as the local source of truth;
- persisted privacy-reduced active forecast target in DataStore;
- explicit FRESH / STALE / EXPIRED cache state;
- cache-preserving refresh failures;
- bounded per-host request pacing and at-most-one eligible transport IO retry;
- explicit user-triggered approximate-location acquisition;
- immediate reduction of device location to the canonical 0.1-degree forecast grid before persistence;
- simple hourly alpha screen with temperature, condition, precipitation, wind and evidence count;
- Russian and English alpha UI;
- release JNI/R8 and vendored native-bundle verification in CI.

## Privacy

The alpha requests only Android coarse location and only after explicit user action. Raw device latitude/longitude is not persisted. See [PRIVACY.md](../../PRIVACY.md).

There is no MeteoOne account system, advertising SDK or analytics SDK in this release line.

## Known alpha limitations

- single-screen diagnostic/product slice rather than the final M3 UI;
- refresh is manual; no background WorkManager scheduling;
- no model-comparison screen;
- no saved-location/profile editor;
- no map/radar view;
- no widgets or alerts;
- no calibrated numeric confidence percentage;
- CodeQL Kotlin analysis remains compatibility-gated and is recorded as skipped rather than treated as passed.

## Canonical release artifact policy

The MeteoOne release pipeline produces, in one signed run from one reviewed source SHA/version:

- `meteoone-<version>.apk` — manual direct-device release acceptance test;
- `meteoone-<version>.aab` — manual RuStore upload only after APK PASS;
- matching R8 mapping, build provenance and `SHA256SUMS` covering all release files.

The APK is not uploaded to RuStore. The AAB is not used for a second functional smoke test. If the paired APK fails acceptance, the AAB is not published.

## Canonical unified `0.1.0-alpha.1` evidence

Canonical release source SHA:

`e8035f648476669e9c839a8a510d02250faf4b18`

- owner request bridge run: `35197083771` — success
- signed release workflow run: `35197092084` — success
- application id: `com.sl.meteoone`
- version name: `0.1.0-alpha.1`
- version code: `1`
- Actions artifact id: `10486141848`
- Actions artifact name: `meteoone-0.1.0-alpha.1-signed-release`
- artifact archive SHA-256: `6289c2e59fab462cabc118eaee7d2322582748260e6e35b9f30a1a612e4bf095`
- APK SHA-256: `eefac07c4b929e2886a6412c20e61a8141bd479b01a71a8fdcd0cfb9074e48f3`
- AAB SHA-256: `604f1987183cdbcc68ac95041c522665c9005a9340934fbea6eba0063194cdac`
- R8 mapping SHA-256: `d267cd082dd2db7deeffb0845b836ce3dee4f19b918949fa19e64d6369b796e0`
- APK build-provenance attestation: `48125618`
- AAB build-provenance attestation: `48125637`
- signing certificate SHA-256: `F0:25:71:C4:07:41:E2:CB:07:15:64:F5:B6:3F:D3:DC:38:A8:75:D0:ED:A1:1A:8C:42:26:9E:D6:35:BC:2A:58`
- signer: exactly one RSA-2048 signer

The unified workflow verified APK v2/v3 signatures, the AAB JAR signature, package/version identity, the expected certificate fingerprint, the required arm64 GRIB native libraries, deterministic checksums, provenance, both attestations and unconditional temporary-keystore cleanup.

The one release bundle records:

```text
apk_role=manual-device-smoke-test
aab_role=manual-rustore-upload-after-apk-pass
not_rustore_delivered_apk=true
```

This canonical unified AAB supersedes the earlier AAB-only artifact as the RuStore submission candidate.

## Manual device acceptance — PASS

The original manual acceptance was performed on the earlier signed APK from application source SHA `ae27269aa753ebcd812922b5023d5f6854810df9`:

- APK workflow run: `35194311112`
- APK Actions artifact id: `10485222565`
- tested APK SHA-256: `9a76361306b29252370cba3e40d8e6ee89c1cd32c103f3b4c99ac445028d5e6c`
- package/version: `com.sl.meteoone`, `0.1.0-alpha.1`, versionCode `1`
- APK v2/v3 signatures: verified
- signers: exactly one
- required arm64 GRIB native libraries: verified
- downloaded artifact checksum/readback: verified

On 2026-09-17 the APK passed the requested manual device acceptance sequence:

1. launch without automatic location prompt — PASS;
2. explicit approximate current-location request — PASS;
3. online forecast fetch/display — PASS;
4. normal forecast content and scrolling — PASS;
5. full app close/restart — PASS;
6. cached forecast restored after restart with network disabled — PASS;
7. offline refresh failure leaves cached forecast visible — PASS;
8. after network restoration, refresh succeeds while existing cache remains visible until replacement — PASS;
9. overall behavior matches the intended M2 offline-first design — PASS.

### Functional payload equivalence to the canonical bundle

The tested historical APK and canonical unified APK have different whole-file hashes because Android embeds the Git revision into release metadata. The functional payload was compared explicitly:

- there are no production Android application/module/build-config changes between the tested source and canonical release source;
- both APKs have the same 237 entry names;
- 236/237 APK entry contents are byte-identical;
- only `META-INF/version-control-info.textproto` differs, containing the corresponding Git revision;
- AAB functional entries are identical; differences are limited to the matching VCS entry and JAR signature metadata recalculated because that signed metadata changed;
- the old and canonical R8 mapping files are byte-identical with SHA-256 `d267cd082dd2db7deeffb0845b836ce3dee4f19b918949fa19e64d6369b796e0`;
- required arm64 native payload is present in both canonical artifacts.

The canonical unified release therefore has the same executable/resource payload that passed device acceptance. Functional acceptance for `0.1.0-alpha.1` remains **PASS**; no second device test is required solely for the VCS metadata update.

For future releases, manually test the exact APK from the canonical unified release bundle before uploading its paired AAB.

## Historical superseded evidence

Kept only for provenance:

- old AAB workflow run `35109403734`, artifact `10451423888`, AAB SHA-256 `b5da2069b7d08d7eec39882aef921702c145d8648fe082ce6c6d0019b7bcd93c`, attestation `47936791`;
- historical device-test APK run `35194311112`, artifact `10485222565`, APK SHA-256 `9a76361306b29252370cba3e40d8e6ee89c1cd32c103f3b4c99ac445028d5e6c`.

Do not upload the superseded old AAB when the canonical unified AAB is available.

## Remaining release gate

The remaining work is external RuStore state only:

1. confirm that RuStore accepts `versionCode = 1` for the MeteoOne package/history;
2. configure/confirm the RuStore application-signing identity and AAB upload-certificate role;
3. complete store metadata, privacy/data-safety declarations and real screenshots;
4. manually upload the exact canonical AAB with SHA-256 `604f1987183cdbcc68ac95041c522665c9005a9340934fbea6eba0063194cdac`;
5. complete RuStore moderation/acceptance for the closed alpha track.

The functional device gate is already PASS and is not repeated for the AAB.

If alpha `versionCode = 1` is accepted/used, every later alpha/public build must use a higher version code.

No GitHub tag or GitHub Release is required for this first closed alpha. Do not start M3 before this alpha boundary is resolved.
